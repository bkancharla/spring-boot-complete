spring:
  cloud:
    discovery:
      enabled: true

    config:
      uri: http://localhost:8888
      username: root
      password: s3cr3t

  profiles:
    active: development
management:
  endpoints:
    web:
      exposure:
        include: "*"
