spring:
  cloud:
    discovery:
      enabled: true

    config:
      uri: http://localhost:8888
      name: apiconfigserver


  profiles:
    active: development
management:
  endpoints:
    web:
      exposure:
        include: "*"
