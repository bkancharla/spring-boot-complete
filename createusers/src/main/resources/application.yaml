spring:
  application:
    name: createusers
  cloud:
    discovery:
      enabled: true

    config:
      uri: http://localhost:8888
      username: root
      password: s3cr3t
  h2:
    console:
      enabled: true
      settings:
        web-allow-others: true
  jpa:
    generate-ddl: true
    hibernate:
      ddl-auto: create
  datasource:
    url: jdbc:h2:~/test

eureka:
  instance:
    instance-id: ${spring.application.name}:${random.value}
---
spring:
  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest
